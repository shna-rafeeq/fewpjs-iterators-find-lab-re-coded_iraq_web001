const testVar = {}

function testFunc() {
  return "hi"
}

function superbowlWin(arr){
 let newarr = arr.find((item)=>{
   
  return object.assign({},)item.result === "W"
 
  })
  return newarr;
}
superbowlWin(record);
